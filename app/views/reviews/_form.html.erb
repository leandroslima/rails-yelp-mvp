<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, error: "Rating from 0 to 5" %>
  <%= f.input :content %>
  <div class="btn btn-primary btn-sm"><%= f.button :submit %></div>
<% end %>
